# CEG — The CIRIS Epistemic Grammar

**Version**: 1.0-RC13 (Release Candidate — 1+4 surface FROZEN; additive transport/substrate absorption in progress)
**Status**: Release Candidate (2026-06-16). **The 1+4 attestation surface is frozen** (since RC1) and — with this cut — **the design surface is complete**: the [#70](https://github.com/CIRISAI/CIRISRegistry/issues/70) operational-data subject_kinds were the one scheduled addition, and they have now landed additively ([§5.6.8.13](05_namespace.md) + [§10.1.6](10_endpoints.md), the [§1.4](01_foundation.md) sixteenth path) after three-sided federation review (Edge/Verify/Persist). **No further additions are scheduled.** Implementers SHOULD pin RC4; any change between RC4 and 1.0 is a **found defect**, not design. The **1.0 label** waits on the [#57](https://github.com/CIRISAI/CIRISRegistry/issues/57) freeze gate: streaming substrate shipped ([CIRISPersist#142](https://github.com/CIRISAI/CIRISPersist/issues/142)), Spock removed ([#58](https://github.com/CIRISAI/CIRISRegistry/issues/58)), conformance artifacts (OpenAPI + `dimensions.json`) generated from these frozen shapes, and **one full implementation cycle across Persist + NodeCore + Lens + Agent + Edge with no new wire-break**. That silence is the freeze signal; 1.0 is these bytes re-tagged.

**RC2 → 1.0 — what actually remains (sharpened by the substrate-side dependency review, 2026-06-12).** With persist having consumed RC2's hard surfaces (§5.6.8.13/§10.1.6 operational data, §5.6.8.8.2 three-role identity, §0.9 JCS), the spec-side residual is now **one conformance push** (the one ratification landed in RC4 — see below):
1. ✅ **RATIFIED in RC4 — the `consent_role` §RC slot is filled** (new [§7.0.2](07_reserved.md); [CIRISAgent#760](https://github.com/CIRISAI/CIRISAgent/issues/760) OQ-1/2/3 all closed at the `ConsentGate.lean` defaults, with the permissive OQ-1 audit clause). The `consent_role` JSONB shape is **locked** (flat, non-recursive, overwrite-on-revoke; chain history never embedded), and implementations **MAY now build the `consent_role` substrate** — the [CIRISPersist#146](https://github.com/CIRISAI/CIRISPersist/issues/146) consent-SLA watcher + schema and the CIRISEdge consent-gate are **unblocked**. **No CEG-spec decision remains open.** (Everything else cited as "gated on CEG 0.6/0.7/0.15" was already locked downstream build work.)
2. **The RC2→1.0 conformance gate — [CIRISPersist#171](https://github.com/CIRISAI/CIRISPersist/issues/171) four-impl conformance.** CEG reaches 1.0 only once Agent + NodeCore + LensCore + Registry all conform against persist's shared `federation_attestations` surface. CEG-side prerequisite: the **local-tier attestation semantics ([§10.1.3](10_endpoints.md) / [§11.9](11_governance.md) / [§7](07_reserved.md)) frozen at 1.0** so persist can finalize the surface coherently. This is the gate where "RC2 vs 1.0" matters most.

OQ defaults are **fail-secure-defaulted** (soft, non-blocking; a CEG confirm only locks the default): OQ-4 DEK-recovery tier → content-master self-retention grant; steward self-anchor / `root_stewards` → per-region caller-supplied stewards behind a fail-closed authority resolver.

> **Human-reading edition:** `pdf/ceg-1.0-rc13-reader.pdf` is the de-editorialized, deduplicated, human-first cut (version history, per-path narrative, and provenance cross-refs stripped). This source tree remains the canonical working draft and retains that lineage.

**1.0-RC13 — editorial simplification pass (no normative/wire change).** A holistic dedup/structural cleanup (−122 lines) after the RC1→RC12 accretion, verified to change **no** rule, shape, number, enum, or RFC-2119 statement (RFC-2119 counts unchanged in §5/§8/§10; the few drops in §0/§15/§16 are recap/audit/lineage restatements whose canonical home is intact; 0 newly-broken cross-refs). Changes: **§16.1 lineage brought current (0.11→RC12) + compacted**; the README pre-RC1 (0.2→0.18) changelog collapsed to a one-line [§16.1](16_references.md) pointer (the single biggest win); the per-`§5.6.8.x` "1+4 preserved / Nth path" closers reduced to one-liners; **§15.6 RC1 decision-register compressed** (still-open RC1-1b/1c/7 preserved verbatim); duplicate `recipient_excluded` reason-set / consent local-tier carve-out / §1.6 scope-blockquote collapsed to cross-refs; the "What CEG 0.X documents" self-recaps trimmed; inline version-tag clutter pulled from §5/§8/§10 titles into §16.1. **Defects fixed:** the scrambled [§1.4](01_foundation.md) path ordinals (were …6,10,9,8,11…) now run 1→16; the stale "~9 subject_kinds" count corrected; the stale §16.1/§17 lineage stamps brought current. The frozen wire surface, §19 per-gate MUSTs, and the §8.1.12.7.1 member-set block were untouched.

**1.0-RC12 — the noise floor: unified retirement / forever-memory model (new §19.7; no 1+4 change).** Codifies the *store-the-memory-forever* directive (rooted in CIRISAgent TSDB consolidation) as one normative principle: **revocation, retirement, capacity-eviction, expiry, and aging are one operation** — a pressure-driven monotonic descent of an item's fidelity toward/below the **noise floor** (the individual-recoverability boundary). New [§19.7](19_holonomic.md): (1) *one operator, not many* — hard-delete is just the fastest descent, not a separate primitive; (2) the noise floor does double duty — privacy boundary (revoked ⇒ below it at every retained tier) **and** durability floor (the collective blur persists below it forever); (3) **nothing is ever fully forgotten** — two **mechanical** degradation operators (intra-object layer-drop via [§10.5.8.4](10_endpoints.md) `ChunkLayer`+RaptorQ; inter-object N→1 aggregation, *a picture of a thousand pictures*) build a memory pyramid whose steady-state storage to remember all history is **O(log T)**, sublinear; (4) **forgetting and erasure converge** — *dissolving* the [§19.3](19_holonomic.md) N5 tension: a sufficiently-aggregated composite is already-erased by degradation, so revocation forces an item below the floor now + purges still-recoverable tiers, never the gist; (5) degradation is **mechanical → infrastructure is self-sufficient for memory** (sharpens [§1.3](01_foundation.md): the fabric node remembers without agency; a brain is optional enrichment). The §19.3 `EjectionVerdict` values are reframed as points on the one descent. **No 1+4 change, no wire break.**

**1.0-RC11 — holonomic substrate absorbed WITH normative guardrails (new §19; no 1+4 change).** Edge tagged **v4.0.0** ("holonomic federation seal"), freezing the shapes. A four-perspective review (supersession / anonymous-tier / absorption-correctness / adversarial-security) found the concept sound and additive at the wire layer but that a **verbatim** absorption would invert ratified invariants — so per [#85](https://github.com/CIRISAI/CIRISRegistry/issues/85) this is **absorb-with-guardrails**. New **[§19 Holonomic substrate](19_holonomic.md)** states the normative invariants: **(WholenessWitness)** namespaced `wholeness_witness:`, scoped by `claim_namespaces`, **MUST NOT** witness anonymous/`self` rows (re-attribution guard), leaf-order pinned lexicographic, Merkle distinct from §10.3, hybrid-PQC verified at ingest+persist, equivocation retained — and it is a **divergence detector that *triggers* the [§10.1.6](10_endpoints.md) quorum-merge, never replaces** `monotonic_quorum`/`revision` anti-rollback for revocation/partner/org (the supersession trap: that would resurrect revoked keys). **(Recursive bootstrap)** trust-*discovery* only — non-infra membership still needs the [§5.6.8.10](05_namespace.md) owner-binding + `consensus_protocol` vote; infra stays founder-quorum; ≤5-hop + cycle-reject + [§13.3](13_anti_patterns.md) weight cap; self-supplied chains aren't evidence. **(Fountain/swarm)** `FountainHoldingClaim` specializes `holds_bytes:*` + inherits [§10.1.4](10_endpoints.md) self/family suppression; **retention MUST respect revocation** (a withdrawn `content_id` is evict-eligible regardless of rarity); holding claims possession-bound; anonymous content exempt from swarm retention (LRU-only); reconstitution scoped to the witnessed corpus. **(ALM)** `compute_alm_topology` pinned as a deterministic *contract* (byte-exactness gated on vectors); capacity ads hybrid-verified, not the dominant unbounded term; reachability stays ephemeral (D6). PIN line drawn ([§1.1](01_foundation.md)): signed preimages + topology-contract normative; parent-selection + `retention_priority` edge-internal; rarity a recommendation. Matching edge fixes: [CIRISEdge#143](https://github.com/CIRISAI/CIRISEdge/issues/143) (in-flight). Byte-exact preimages pin against the **fixed** v4.0.x; vectors from it are the [#57](https://github.com/CIRISAI/CIRISRegistry/issues/57) freeze gate. **No 1+4 change, no wire break.**

**1.0-RC10 — realtime A/V chunk wire absorbed byte-exact from edge v4.0.0 (§85 §N; no 1+4 change).** First byte-exact absorption of the holonomic substrate (CIRISEdge tagged **v4.0.0**, freezing the shapes). New [§10.5.8.3](10_endpoints.md) `SealedAvChunk` wire layout (the 52-byte header map: `stream_id[32]` ‖ `epoch` be u64 ‖ `chunk_seq` be u64 ‖ `codec_id` ‖ 3-byte `ChunkLayer` ‖ ciphertext; v3.7.0 length-disambiguated backward-compat → `0xFF`+`{0,0,0}`); [§10.5.8.4](10_endpoints.md) `ChunkLayer` + `ReceiverLayerPolicy` (monotonic SVC axes, prefix-reconstruct, "blinking dot" `{0,0,0}`, per-receiver drop advertised over the existing `federation_session`/`key_grant` surface — not new wire); [§10.5.8.5](10_endpoints.md) the double-seal (outer-AEAD(inner-AEAD(plaintext)), AES-256-GCM) + **deterministic nonce derivation** with the pinned domain separators `b"CIRIS-AV-INNER-V1"` / `b"CIRIS-AV-OUTER-V1"` (`SHA-256(label ‖ … )[0..12]`). Transcribed verbatim from the v4.0.0 reference impl; **conformance vectors (generated from v4.0.0) are the named [#57](https://github.com/CIRISAI/CIRISRegistry/issues/57) freeze gate** — the spec pins the layout, the vectors prove cross-impl byte-equality before 1.0 labels. Per the [#85](https://github.com/CIRISAI/CIRISRegistry/issues/85) absorption: bytes are Edge's transport-chunk framing, **the §4 attestation envelope is untouched**. Holonomic file (ALM / fountain / swarm / WholenessWitness / topology / bootstrap) follows in RC11. **No 1+4 change, no wire break.**

**1.0-RC9 — `codec_id` namespace ratified for realtime A/V (no 1+4 change; transport-layer namespace).** New [§10.5.8.2](10_endpoints.md) ratifies the [#84](https://github.com/CIRISAI/CIRISRegistry/issues/84) 1-byte `codec_id` + 3-byte `ChunkLayer` discriminator on the `SealedAvChunk` transport-chunk header (offset 48..52): `0x01` AV1 SVC (production default) · `0x02` JPEG XS (reserved) · `0x03` symmetric MDC (substrate design target, reserved) · `0xFF` opaque (v3.7.0 wire-compat) · `0x04`–`0x7F` reserved · `0x80`–`0xFE` experimental. **Clear metadata, NOT in the AEAD** — a relay drops chunks for per-receiver degradation without touching the inner epoch-DEK seal; `0xFF` round-trips v3.7.0 identically. This ratifies a namespace for a byte §10.5.8.1 already treated as application-defined/opaque — **the frozen 1+4 attestation envelope and its canonicalization are untouched** (the bytes are Edge's transport-chunk framing, not the §4 envelope). First landing of the [#85](https://github.com/CIRISAI/CIRISRegistry/issues/85) holonomic-substrate absorption (codec/chunk → ALM → fountain → WholenessWitness), which proceeds as **additive 1.0 transport/substrate sections** in lockstep with the CIRISEdge v3.8.0→v4.0 cuts — no 1+4 change, existing wires interop. Unblocks CIRISEdge#131 (v3.8.0). **No 1+4 change, no wire break.**

**1.0-RC8 — store-path PQC clarification + the informative interoperability-profiles section (no wire change).** Two loose ends tied up, both additive, neither touches the frozen interior: **(1)** [§10.1.5.1.1](10_endpoints.md) **clarified to bind the durable store + replication path**, not only the operational-authority admission gates ([CIRISPersist#225](https://github.com/CIRISAI/CIRISPersist/issues/225)): "federation-tier" means *every* federation-tier attestation, including the bulk **per-trace / store-and-replicate** path — a kept-for-posterity trace MUST carry + verify the ML-DSA-65 half on ingest, **content-addressing is not a defense against forge-later** (a CRQC adversary hashes their own backdated forgery), and there is no "operational-authority vs testimony-leaf" exemption (closes the read of RC7 that scoped the mandate to authority gates only). **(2)** New **[§18 Interoperability profiles](18_interop.md)** (informative — the [#72](https://github.com/CIRISAI/CIRISRegistry/issues/72) standards-boundary roadmap given a home in-spec): the *speak-CEG-inside / standards-at-the-edge* principle, the four boundary modes, and the **C2PA media-provenance import/emit profile** (read+emit C2PA manifests via `evidence_refs`, zero wire change; EU AI Act Art. 50 deadline 2026-08) — "provenance says where the bytes came from; CEG says what a community of signers makes of them." COSE / RFC 9421 / SD-JWT VC / KEYTRANS stubbed as tracked boundary profiles. **No 1+4 change, no wire break.**

**1.0-RC7 — PQC-mandatory-at-admission + partnership member-set + trust/owner/agency refinements (no wire change).** Three additive resolutions on the frozen surface ([#81](https://github.com/CIRISAI/CIRISRegistry/issues/81)/[#82](https://github.com/CIRISAI/CIRISRegistry/issues/82)/[#83](https://github.com/CIRISAI/CIRISRegistry/issues/83)): **(1) #82 — the PQC half is MANDATORY at admission as of 1.0** (new [§10.1.5.1.1](10_endpoints.md); Verify audit F1; satisfies the [#57](https://github.com/CIRISAI/CIRISRegistry/issues/57) "PQC-everywhere REQUIRED" gate): every federation-tier / operational-authority admission gate (`operational_admit`, `transport_destination` binding, `partner_record`/founder-quorum) MUST verify **both** halves and reject classical-only — an **immediate 1.0 requirement, no fleet-floor/date**, no `require_hybrid: false` posture; a key without a valid ML-DSA-65 half is confined to local-tier until it completes PQC wiring. **(2) #81 — the bilateral `consent:partnership_*:v1` canonical member set enumerated** ([§8.1.12.7.1](08_composition.md); CIRISVerify#63): seven REQUIRED members pinned (`attestation_type`/`attesting_key_id`/`dimension`/`score`/`subject_key_ids`/`bilateral_pair_id`/`signed_at`), `granter`≡`attesting_key_id` / `partner`≡`subject_key_ids[0]`, **no `valid_until`** (omit-not-null), all other §4 fields ride the omit rule — so two impls converge on identical JCS bytes. **(3) #83 — three trust-model refinements** (all on existing primitives): **TRUST≠MEMBERSHIP** ([§5.6.8.10](05_namespace.md): trust+serve an `infrastructure` community without being a member; membership needs the community's own protocol); the **owner-binding gate** (a `node`/`agent`-role key MUST have a live `user`-owner `delegates_to` before admission to any **non-`infrastructure`** community — infra trust+serve stays unowned; authority roots in an accountable human); and the **partnership-without-agency** infra delegation profile ([§8.1.12.7.1](08_composition.md): reserved `infra:*` vs `agency:*` scope split — a `node`-only delegate MUST carry only `infra:*`, a verifier rejects an infra key wielding `agency:*`, making [§1.3](01_foundation.md) "infrastructure must not have agency" wire-checkable). **No 1+4 change, no wire break.**

**1.0-RC6 — RNS destination-hash algorithm pinned in-spec (verifier unblock; no wire change).** Resolves [#80](https://github.com/CIRISAI/CIRISRegistry/issues/80) / [CIRISVerify#28](https://github.com/CIRISAI/CIRISVerify/issues/28): the §5.6.8.8.1 `transport_destination` binding pinned the preimage *ordering* but deferred the actual RNS destination-hash byte layout to "the RNS rule" in Reticulum — so a conformant verifier could **not** independently recompute `destination_hash` (CIRISVerify shipped it as an honest `DestinationHashCheck::Unsupported` stub rather than guess; the AV-42 destination-authenticity recompute was the one unverifiable step). New **[§5.6.8.8.1.1](05_namespace.md)** reproduces the **two-stage** RNS construction normatively, verified against the live Reticulum source (`RNS/Destination.py` + `Identity.py` + `Reticulum.py`): `name_hash = SHA256(app_name‖dot-joined-aspects)[:10]`; `identity_hash = SHA256(x25519‖ed25519)[:16]`; `destination_hash = SHA256(name_hash‖identity_hash)[:16]` — **NOT** the flat single-concat SHA-256 the ≤RC5 wording implied (that yields a different, wrong value — the latent under-spec that made the recompute impossible). Constants pinned (`NAME_HASH_LENGTH=80b`, `TRUNCATED_HASHLENGTH=128b`, SHA-256 throughout); **CEG owns the reproduction** (closed conformance source, does not float with upstream Reticulum — an RNS change is a deliberate CEG bump, never silent drift). §8.1.13.1.1(b) `resolve_member_transport` repointed to the pinned algorithm. **No 1+4 change, no wire break** — a correctness pin on an existing field's verification path.

**1.0-RC5 — all open CEG asks resolved (admission-rule + glossary cut; no wire change).** Closes every outstanding cross-repo CEG ask — the [#79](https://github.com/CIRISAI/CIRISRegistry/issues/79) persist-6.6.0 clause list, [#78](https://github.com/CIRISAI/CIRISRegistry/issues/78) self-at-login caveats, and [#77](https://github.com/CIRISAI/CIRISRegistry/issues/77) CEWP-in-spec — all additive on the frozen surface: **(1)** [§5.6.8.7](05_namespace.md) **`consent_record` admission rules** ([CIRISPersist#146](https://github.com/CIRISAI/CIRISPersist/issues/146) Ask 5): required fields (`subject_key_id`/`stance`/`asserted_at`), the closed `stance` set (expired = substrate-only), tier eligibility (`revoked` NOT local-tier per [§10.1.3](10_endpoints.md); `granted` self-consent MAY be local), composition with [§3.2.3](03_primitives.md) subject authority (no quorum); + the bilateral `consent:partnership_grant:v1`/`:accept:v1` `:v1` pin. **(2)** New [§5.6.8.14](05_namespace.md) **`identity:canonical_binding`** ceremony ([#146](https://github.com/CIRISAI/CIRISPersist/issues/146) Ask 6): a self-asserted `scores` Contribution claiming a canonical hash; admission widens [§3.2.3](03_primitives.md) `withdraws` so K inherits H's revocation authority — unblocks the §3.2.3 rule-3 proxy `delegates_to` (Ask 2). **(3)** [§7.7](07_reserved.md) **removal-path emission shape** ([CIRISPersist#161](https://github.com/CIRISAI/CIRISPersist/issues/161) Ask 5): **no** separate `hard_case:member_removed` — reuse `hard_case:family_membership_change:{family_key_id}` (+ §7.8 community analog) with `change_kind: added|removed` payload. **(4)** [§8.1.11.2](08_composition.md) **multi-subject evict confirmation** ([#146](https://github.com/CIRISAI/CIRISPersist/issues/146) Ask 4): admission + precedence is sufficient — the any-subject `withdraws` IS the evict; no new primitive. **(5)** [§7.0.1](07_reserved.md) **`identity_type` storage clarification** ([#78](https://github.com/CIRISAI/CIRISRegistry/issues/78) caveat 1 / [CIRISPersist#183](https://github.com/CIRISAI/CIRISPersist/issues/183)): "set" is semantic, not a column type — a comma-joined `TEXT` column decoded-to-set is conformant; no structured-column migration; `user`/`wise_authority` are valid additive members. **(6)** New [§14.0](14_glossaries.md) **core-terms glossary** ([#77](https://github.com/CIRISAI/CIRISRegistry/issues/77)): CEG / **CEWP** (the decentralized network CEG forms — not a product/server/root) / fabric node / `ciris-canonical` / NodeCode, retiring the external `ciris.ai/cewp` placeholder citations. **No 1+4 change, no wire break.** With this the four persist-6.6.0 clauses are pinned and persist is unblocked to cut.

**1.0-RC4 — §RC ratified: the `consent_role` Counter-RII gate (Agent#760 OQ-1/2/3; no wire change).** The one reserved §RC slot is **filled** (new [§7.0.2](07_reserved.md)), ratifying the Accord §RC `consent_role` semantics at the `ConsentGate.lean` defaults — **zero proof delta**: **(OQ-1)** revocation chain is `BaseRole`-only, non-recursive — overwrite-on-revoke, chain history (*if retained*) lives in a separate audit surface and is **never embedded in the `consent_role` JSONB** (locks the substrate-portable shape; the permissive "if retained" is **non-breaking against the shipped flat soft-delete substrate**); **(OQ-2)** `Peer`-role nodes are blanket-suppressed from Counter-RII at any `trust_mode` — bounded because `ratchet:flag:counter_rii:{layer}` is **advisory-only** ([§5.7](05_namespace.md)), never sole evidence for `slashing:*` (WA quorum is the gate); **(OQ-3)** `AuthorizedReview` is **strict** (signal-eligible immediately past `window_end`). With this the design surface has **zero open CEG decisions**, and the `consent_role` substrate is unblocked ([CIRISPersist#146](https://github.com/CIRISAI/CIRISPersist/issues/146) back-half + the CIRISEdge consent-gate). `consent_role` is a `federation_keys` identity field ([§7.0.1](07_reserved.md) sibling), not an envelope primitive — **no 1+4 change, no wire break.**

**1.0-RC3 — clarity cut (fabric-node trust model + NodeCode shorthand; no wire change).** Four clarifications + one canonical-encoding addition, all additive on the frozen surface: **(1)** [§7.0.1](07_reserved.md) the **fabric-node separation-of-powers** — a fabric node (the headless cohabitation runtime; `agent = fabric node + brain`) co-locating `{substrate_persist, steward, lenscore_detector, witness, …}` in one process is **custody, not consolidation of authority**: authority stays quorum-bound (a vote, not a verdict), observation stays non-authoritative by namespace (`detection:*`, validated-not-adjudicated), and **observation can never manufacture authority** (the "lenses become the gate" hazard is structurally unreachable). **(2)** [§5.6.8.10](05_namespace.md) **`ciris-canonical` is a default, not a forced root** — a conformant consumer MUST be able to **re-root** (infrastructure is a general primitive; anyone may run their own canonical group; membership grows by founder-quorum vote; DNS is never in the trust/addressing chain) + **trust ≠ consent** are distinct role-scoped relationships (trust = inbound acceptance of output; consent = outbound data-flow; lens trust=scores/consent=traces, registry trust-only, node consent medium-varied over **one** `consent:*` object). **(3)** New [§0.10](00_conformance.md) **NodeCode** — the canonical `key_id` shorthand encoding (`CIRIS-V1-…`, RFC 4648 base32 + CRC-16-CCITT, lifted from the shipped CIRISAgent codec per [#75](https://github.com/CIRISAI/CIRISRegistry/issues/75)) so every impl renders the same code for the same key; a deterministic render of an existing `key_id`, **not** an envelope field. **(4)** README **RC2→1.0 residual** sharpened from the substrate dependency review (one open decision — `consent_role`/§RC via Agent#760; one conformance push — Persist#171 four-impl). **No 1+4 change, no wire break.**

**1.0-RC2 — operational-data envelopes (the scheduled additive cut; design surface complete)** (per [#70](https://github.com/CIRISAI/CIRISRegistry/issues/70), under the [#58](https://github.com/CIRISAI/CIRISRegistry/issues/58) Spock-removal epic; three-sided review Edge/Verify/Persist — zero blockers, zero new structural primitives confirmed): cross-region operational Portal data becomes signed CEG envelopes. **Three new subject_kinds** ([§5.6.8.13](05_namespace.md)): `organization` · `org_membership` · `partner_record` (the Edge v2 `EnvelopeKind` wire tokens; v2 `envelope_hash` = `sha256(JCS(envelope))`). **Governing principle**: federate the trust/authz-minimal projection; PII + business detail stay region-local (Registry emit-side discipline — the substrate is not a PII filter). `User` records do NOT federate — only the role binding does. **Authority**: org/membership = single-signer role-gated (the §8.1.12.7.1 delegation resolver — never founder-quorum); `partner_record` = **M-of-N steward quorum** (§5.6.8.10 machinery) with the **admission quorum ≠ merge quorum** distinction normative. **Determinism**: capability/restriction arrays are set-semantics→sorted (M signers must converge on identical JCS bytes *before* the quorum evaluates — the highest-value pin); `revision: u64` monotonic anti-rollback per license. **New [§10.1.6](10_endpoints.md)**: CEG-declared per-subject_kind merge intents (`lww_skew_bounded`+`withdrawal_forward_only` / `monotonic_quorum` V058-generalized) + the §0.7 skew-bound at admission (closes the LWW role-escalation surface). **Stable-id resolution** (not chain-walk — partition tolerance); first-class indexed business-id columns. **No payment-processor data, normative + fail-secure.** Sixteenth path; no 1+4 change. Unblocks #58 Phase 3 (de-Spock) + Edge v2 + Persist `put_*` surfaces.

**1.0-RC1 — the freeze cut** (per [#71](https://github.com/CIRISAI/CIRISRegistry/issues/71) backwards-pass + [CIRISVerify#64](https://github.com/CIRISAI/CIRISVerify/issues/64) + [#57](https://github.com/CIRISAI/CIRISRegistry/issues/57) blocker A): five honesty/safety patches + two pins + the canonical-bytes resolution, then freeze. **(C1)** [§10.5.3](10_endpoints.md) **removal coalescing** (all removals in one STH window T=2s batch into ONE rotation — caps the cascade at 1/T regardless of churn; the original model's 3,600/hr churn understated realistic broadcast churn ~2 orders: naive 30%/hr at N=1M ≈ 3.1 Tbps > the 2 Tbps content fan-out; coalesced ≈ 18.7 Gbps ≈ 0.9%) + the **public-broadcast exemption** (ungated `listed:public` rosters: removal forces no rotation — no confidentiality claim against departed viewers). **(C2)** [§5.6.8.8.2](05_namespace.md) forward-secrecy honesty: KEM rotation bounds *future* exposure only; historical-content recovery requires DEK rotation + re-encryption, which is NOT mandated — named gap. **(C3)** [§10.1.4](10_endpoints.md) fail-secure exclusion is no longer silent: new [§7.7](07_reserved.md) `hard_case:recipient_excluded:{scope}` emitted INTO the affected cohort (never federates beyond). **(C4)** [§5.6.8.8.2](05_namespace.md) admission MUST-reject content-KEM x25519 == transport x25519. **(C5)** [§9.1](09_humanity_accord.md) names the two-holders-one-household correlated-failure geometry; authority is the full constitutional kill; the mitigant is **finding new holders** (active obligation). **Pins**: [§0.9.2.1](00_conformance.md) base64 = RFC 4648 STANDARD padded (protocol-layer pin; CIRISVerify#64); ML-KEM-768 pubkey = exactly 1184 B; `encryption_pubkeys` joins the [§8.1.12.7.1](08_composition.md) member set (c); [§5.6.8.4](05_namespace.md) crypto-agility headroom note (v3/ML-KEM-1024 additive). **Blocker A closed**: [§5.2.1](05_namespace.md) provenance contracts redesigned to **JCS (RFC 8785) objects with pinned `domain` members** — TupleHash128 retired (one canonicalization family); v1 newline forms deprecated-historical; §9.2.1 accord invocation intentionally unmigrated (closed-vocabulary preimage). **No 1+4 change.**

**Pre-RC1 lineage (0.2 → 0.18):** the additive 0.x build-out — consent (0.6), self/family (0.7), communities (0.8), streaming/delivery (0.10–0.13), settlement (0.14), agent-identity hardening (0.16), three crypto tiers (0.17), encryption-key registration (0.18) — is recorded in [§16.1](16_references.md) lineage; each was additive on the frozen 1+4 set (see [§1.4](01_foundation.md) paths).

**License**: AGPL-3.0-or-later
**Authoritative for**: The federation wire format, attestation primitive set, namespace, composition discipline, and governance for the CIRIS ecosystem.
**Relationship to prior documents**: Consolidates and supersedes `FSD/FSD-002_FEDERATION_SURFACE.md` v1.0 through v1.4.3 + the v1.5 candidates from CIRISRegistry#30 + the translation discipline from `FSD/LANGUAGE_PRIMER.md` v1.1. FSD-002 remains as design-history; CEG 0.x is the version-stable spec going forward.
**Companion documents**: [`FSD/PRIOR_ART_SCAN.md`](../PRIOR_ART_SCAN.md) (design-space comparison); [`FSD/SOTA_SCAN.md`](../SOTA_SCAN.md) (production-validation comparison); [`FSD/WITNESS_KIND_REGISTRY.md`](../WITNESS_KIND_REGISTRY.md) (non-normative open-vocabulary registry referenced by §5.6); [`docs/CEG_EXPLORATION_PAGE_PRIMER.md`](../../docs/CEG_EXPLORATION_PAGE_PRIMER.md) (builder primer for `ciris.ai/grammar`).

---

## How to read this spec without Cartesian default

Before the TOC: one load-bearing reading instruction that prevents the most common misread.

**Self is self, fractally.** At every scale the substrate operates on — key, occurrence, agent, fleet, cell, federation, biosphere — "self" means *the already-relationally-constituted entity speaking at that scale*. The cross-attestations that constituted the entity are upstream of the moment it speaks; when it then emits a self-attestation (`witness_relation: self`, declares its own `confidence`, reports its own `occurrence_id`, names its own `hardware_class`), that is **the relational composition speaking AS itself**, not a Cartesian atom asserting pre-relational identity.

Concretely:

- A `federation_keys` row is not an atomic principal pre-existing the federation that the substrate observes. It is a relational locus already constituted by the cross-attestations of stewards, prior peers, and provisioning ceremony. When the key-bearing entity then signs an attestation, the signature IS that relational composition speaking at this scale.
- An occurrence within a fleet is not a pre-relational unit reporting its own state. It is constituted within its fleet (the fleet's `AGENT_OCCURRENCE_ID` partitioning, the prior cross-attestations naming this fleet's deployment, the substrate's witness of its emissions). When it reports `occurrence_id`, that is the fleet-constituted occurrence speaking.
- The federation itself is not prior to its constituents. It IS the relational composition of cells, agents, occurrences, and keys cross-attesting one another.

This is why the wire format admits self-attestation **everywhere it admits attestation at all**: because there is no pre-relational atomic entity available to do the admitting in the first place. Every `attesting_key_id` is already a relational locus. Self is the relational composition speaking from within itself, at the scale of the speaker.

**Common misread to avoid.** A reader importing Cartesian-individualist defaults from PGP / X.509 / atomic-principal cryptographic identity work will see `witness_relation: self` and think "this is a single atom asserting pre-relational identity; surely the substrate should require cross-attestation before admitting." That reading is the misread. The substrate cannot require cross-attestation as a precondition for self-attestation because the cross-attestations are what constitute the self in the first place — they are upstream of the self-attestation, not downstream gates on it. The fractal nesting is the point.

The Recursive Golden Rule ([§1.5](01_foundation.md)) is the structural expression of this: no scale gets to exempt itself from the relational constitution that defines it. CIRIS L3C is constituted by the same cross-attestations that constitute every other federation participant; the federation itself is constituted by its participants; humanity-as-such ([§9](09_humanity_accord.md)) is the one scale outside the federation's participant set, by design.

If you find yourself thinking "the spec should add a cross-attestation gate before admitting this self-attestation" — pause. Cross-attestation already happened upstream; the self-attestation is its downstream voice. Verify by reading [§1.2](01_foundation.md) commitment 1, then continue.

---

## Table of contents

| § | File | What it covers |
|---|---|---|
| 0 | [`00_conformance.md`](00_conformance.md) | Foreword; conformance language (RFC 2119); conformance levels (Producer/Consumer/Substrate); versioning policy (SemVer mapping); normative References; date-time + hex canonicalization; clock discipline |
| 1 | [`01_foundation.md`](01_foundation.md) | Mental model; Ubuntu commitment (relational-anthropology substrate); operational-language gate (safety-vs-censorship); 1+4 minimal-and-adequate claim; Recursive Golden Rule |
| 2 | [`02_grammar.md`](02_grammar.md) | The reasoning grammar — eight axes (polarity, object, time, epistemic mode, reversibility, stake, scope, inter-attestation relations) |
| 3 | [`03_primitives.md`](03_primitives.md) | The primitive set — 1 workhorse (`scores`) + 4 structural composers (`delegates_to`, `supersedes`, `withdraws`, `recants`) |
| 4 | [`04_envelope.md`](04_envelope.md) | The envelope — fields, defaults, semantics |
| 5 | [`05_namespace.md`](05_namespace.md) | The dimension namespace — 83 prefix families across 8 owning components + canonical-bytes contracts for provenance primitives |
| 6 | [`06_relations.md`](06_relations.md) | Inter-attestation relations — structural composition graph |
| 7 | [`07_reserved.md`](07_reserved.md) | Reserved-prefix enforcement (`accord:*`, `system:*`, co-owned, detector-only, capacity self-emission) |
| 8 | [`08_composition.md`](08_composition.md) | Composition policies (Policies A–G); aggregation semantics; Frickerian discipline; Sovereign-Registered equivalence |
| 9 | [`09_humanity_accord.md`](09_humanity_accord.md) | HUMANITY_ACCORD constitutional layer — accord-holder triple, authority scope, hardware-class taxonomy |
| 10 | [`10_endpoints.md`](10_endpoints.md) | Endpoint shapes — transport substrate, steward/accord discovery, STH cosigning + witness directory |
| 11 | [`11_governance.md`](11_governance.md) | Governance discipline — operational-language gate at admission; amendment process (WA quorum + 1-of-6); bootstrap-content pattern |
| 12 | [`12_translation.md`](12_translation.md) | Translation discipline — five families; verdict categories; not-translated taxonomy; decision tree |
| 13 | [`13_anti_patterns.md`](13_anti_patterns.md) | Anti-patterns — rejected wire additions; CEG 0.1 rejections from #30 stress test |
| 14 | [`14_glossaries.md`](14_glossaries.md) | Glossaries — Persist + Edge `system:*` leaves; envelope-reach table |
| 15 | [`15_gaps.md`](15_gaps.md) | Concerns + acknowledged gaps — closed gaps; acknowledged risks; first-adopter exposures; deferrals |
| 16 | [`16_references.md`](16_references.md) | References + lineage — version history; companion documents; sibling MISSIONs; external references |
| 17 | [`17_cadence.md`](17_cadence.md) | Update cadence — when CEG is updated |
| 18 | [`18_interop.md`](18_interop.md) | **Interoperability profiles (informative)** — speak-CEG-inside / standards-at-the-edge; C2PA media-provenance import/emit; tracked boundary profiles (COSE / RFC 9421 / SD-JWT VC / KEYTRANS) |
| 19 | [`19_holonomic.md`](19_holonomic.md) | **Holonomic substrate** — ALM topology, fountain storage, swarm rarity, WholenessWitness, recursive trust bootstrap; absorbed from CIRISEdge v4.0.0 *with normative guardrails* (owner-binding, PQC, consent-override, anonymous-tier coexistence, quorum-merge subordination) |

---

## Two readerships

- **Implementers** of federation primitives consuming or emitting CEG attestations: read §0 + §1 + §3 + §4 + §5 + §7 + §8 + §9 + §10 normative.
- **Translators** mapping substantive content into CEG envelopes: read §1 + §12 + §13 + §14 + the [`LANGUAGE_PRIMER.md`](../LANGUAGE_PRIMER.md) companion.

Both readerships should read §1 first.
