# CIRISRegistry Ansible Requirements
# Install with: ansible-galaxy install -r requirements.yml
---
collections:
  - name: amazon.aws
    version: ">=7.0.0"
  - name: community.docker
    version: ">=3.0.0"
  - name: community.general
    version: ">=8.0.0"
